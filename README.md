# Agent React Task

This repo stores a reusable repository analysis guide.

`agent.md` tells an agent how to inspect any software repo, understand its stack and structure, assess code quality, and write a repo-specific report named `<repo_name>_agent.md`.

`solehead_agent.md` is an example report created from the Solehead Flutter app.

